// Page
pub struct Page {
    _name: String,
}

pub enum PageSide {
    Front,
    Back,
}

// Page brainstorm
// - Name
// - Type (natus, metafora, bellum, sin, piety, etc.) -- BookArtType
// - Front text
// - Back text
// - (Primary) Action
// - Secondary Action (optional)
// - Passive (optional)
// -
// To call the TUI page, pass the page + side (front-back)?
